import React from "react";

import { Container, ConfirmButton } from "./styles";

const Button = params => {
  const {} = params;

  return (
    <Container>
      <ConfirmButton />
    </Container>
  );
};

export default Button;
